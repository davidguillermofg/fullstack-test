# Opcional. Si no se define, en `npm run dev` se usa el proxy de Vite (/api → localhost:8080).
# Debe incluir /api/v1 al final (o solo .../api y se completará con /v1).
#
VITE_API_URL=http://localhost:8080/api/v1